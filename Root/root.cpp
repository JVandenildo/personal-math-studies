#include <iostream>
#include <vector>
using namespace std;

int root(int a, int b){
    int r;

    return r;
}

int main(){
    int a, b;
    cout << "Base number: ";
    cin >> a;
    cout << "Root number: ";
    cin >> b;

    // only works with perfect roots
    cout << "Root of " << a << " by " << b << " is " << root(a, b) << ".\n";

    return 0;
}